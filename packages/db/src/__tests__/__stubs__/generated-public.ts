// Stub for @signflow/db generated public Prisma client.
// The real file is produced by `prisma generate` and is not committed.
export class PrismaClient {
  $disconnect() { return Promise.resolve(); }
}
